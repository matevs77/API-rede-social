spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/rede_social
    username: rede_social
    password: rede_social
  data:
    redis:
      host: localhost
      port: 6379
  jpa:
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

app:
  cors:
    allowed-origins: http://allowed-origin.com
  jwt:
    secret: test-secret-key-with-at-least-256-bits-for-jwt-signing-ok
